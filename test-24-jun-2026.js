
// Store a string "JavaScript" and print each character using a for loop.

// Count total number of characters in a string "Hello World" using a loop.

// Print a string in reverse order using a loop.

// Count how many times letter 'a' appears in "banana" using a loop.

// Convert all characters of "hello" to uppercase manually using loop (without .toUpperCase()).

// Store "developer" and print only characters at even positions.

// Join two strings "Hello" and "JS" without using + inside print (store result using loop).


// Print all elements of array [1, 2, 3, 4, 5] using a loop.

// Find sum of array elements [10, 20, 30, 40] using a loop.

// Count total elements in array [5, 8, 6, 2] without using .length.

// Find largest number in array [4, 9, 2, 7] using only loop and variables.

// Find smallest number in array [12, 5, 9, 1].

// Reverse an array [1, 2, 3, 4] using loop.

// Print only even numbers from array [1, 2, 3, 4, 5, 6] (without if).

// Copy elements of one array into another array using a loop.


// Use Math.sqrt() to print square root of numbers from 1 to 10 using loop.

// Print random numbers between 1 and 5 using Math.random() inside a loop (5 times).

// Store decimal numbers [2.3, 4.7, 5.1] and round them using Math.round() in a loop.

// Find power of numbers from 1 to 5 using Math.pow() (square each number).

// Use Math.abs() to convert negative numbers [-1, -5, -3] into positive using loop.